class Data {
  bool isBack = false;
  bool isBackFromAddTaskScreen = false;
  int lastFocusedScreen = 0;
  int settingScreenIndex = -1;

  Data({this.isBack, this.isBackFromAddTaskScreen, this.lastFocusedScreen, this.settingScreenIndex});
}