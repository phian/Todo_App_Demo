class MainScreenData {
  bool isBack = false;
  bool isBackFromAddTaskScreen = false;
  int lastFocusedScreen = 0;
  int settingScreenIndex = -1;

  MainScreenData({this.isBack, this.isBackFromAddTaskScreen, this.lastFocusedScreen, this.settingScreenIndex});
}